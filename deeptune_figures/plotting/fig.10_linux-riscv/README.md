# Experiment for Figure 10

Figure 10 of the paper shows the memory consumption improvement over time for a RISC-V application.

## Generating the Figure

To generate the figure, run the following command in the `scripts/` directory:

```bash
python plot_ai_improvement_over-time_wayfinder-opt_riscv.py
```
